# Enseignements
Ce qui a marché, ce qui a échoué, les frictions de la chaîne. Alimenté après chaque
lancement et chaque friction rencontrée.

| Date | App/Phase | Problème ou succès | Enseignement | Action corrective |
|---|---|---|---|---|
| 2026-08-21 | Phase 1 (scrapers) | Les scrapers `tools/scrapers/*.js` utilisaient l'ancienne API CJS de `google-play-scraper` ; la v10 installée est ESM avec export `.default` → `gplay.search is not a function` | Toujours vérifier la version d'une lib scraper avant exécution ; l'API change entre majeurs | Corrigé : `require('google-play-scraper').default` ajouté sur les 5 scrapers GP. Documenter dans un check-setup |
| 2026-08-21 | Phase 1 (scrapers) | `top-charts.js` v10 ne expose que TOP_FREE/TOP_PAID/GROSSING (pas NEW_FREE/NEW_PAID) | Ne pas dépendre des collections « nouveautés » en v10 | Détection des nouveautés via le champ `released` des apps des charts |
| 2026-08-21 | Phase 1 (scrapers) | `scrape-search.js` ne renvoie pas le champ `installs` (uniquement via `scrape-app.js`) | La recherche sert à la découverte + mots clés ; les volumes s'obtiennent app par app | Toujours compléter par `scrape-app.js` sur les leaders avant de scorer |
| 2026-08-21 | Phase 1 (Appfigures) | Le plan « Free Forever » liste des features (ratings, ranks, reviews, public) MAIS sans l'add-on payant **Public Data API**, tout endpoint sur une app non possédée renvoie 403 « Partner API Access required » / 404 | La liste `accessible_features` du root ne garantit pas l'accès réel aux données concurrentes ; il faut activer l'add-on en crédits | Testé réellement : seul `/` répond. Pour la recherche concurrentielle, utiliser les scrapers gratuits (google-play-scraper + app-store-scraper) qui donnent la même donnée. Réévaluer Appfigures uniquement pour les apps du compte ou si l'add-on est activé |
| 2026-08-21 | Phase 1 (App Store) | `scrape-app-ios.js` (app-store-scraper) : le volume de notes s'appelle `reviews` (pas `usersRatingCount`), `score` = note moyenne ; `released`/`updated` en ISO | Connaître le schéma de réponse avant de scorer | `ios-search.js` + `ios-reviews.js` créés ; `reviews` = compteur de notes |
| 2026-08-30 | Phase 1 (mots-clés) | Autocomplétion (pools) ≠ volume : les 188k suggestions sont surtout du bruit (SP 5, phrases jamais tapées) | Ne jamais générer la longue traîne depuis l'autocomplétion ; RÉCOLTER les mots où des apps rankent réellement | Adopté REVERSE KEYWORD MINING (règle 16 AGENTS.md) |
| 2026-08-30 | Phase 1 (mots-clés) | Le reverse keyword mining (organic keywords des apps leaders) donne ~105k mots-clés avec volume+concurrence réels en 20 clusters | C'est LA méthode la plus efficace pour trouver des mots à volume ; chaque mot a pop/comp/rank | Mining par cluster dans `mots-cles/` ; tout donner en CSV brut, ne pas filtrer |
| 2026-08-30 | Phase 1 (Appfigures) | L'API interne `_start/api/aso/products-snapshot/keywords` (session navigateur) expose TOUS les organic keywords d'une app ; le bon pid iOS = `member_product_ids[0]` si `storefronts` contient `apple:ios` | Un mauvais pid → 400 "Request must have applicable product" ; valider chaque pid avant mining | Script Node `_mine_*.js` ; cookies de session Appfigures |
| 2026-08-31 | Phase 2 (avis App Store) | L'extraction avis via Playwright sur `apps.apple.com/...?see-all=reviews` : le rating réel est dans `ol.stars[aria-label="N Stars"]`, PAS dans le nombre d'éléments `svg` (retournait 23) | Vérifier le sélecteur du rating ; `h1` = appName à vérifier avant extraction (navigateur partagé = risque de page parasite) | Extraire `ol.stars` aria-label ; toujours vérifier `appName` + `document.title` avant de sauvegarder |
| 2026-08-31 | Phase 2 (revenus) | La search API Appfigures `_start/api/unified-apps/search?expand=...,estimates` donne revenue/downloads estimates par app via `fetch` depuis la page connectée | Les estimates sont mensuelles (juil. 2026) et fiables pour comparer les clusters ; certaines apps B2B (PayByPhone) n'ont pas de revenue estimé | Query = nom exact de l'app, `count=2` pour prendre la bonne entrée (homonymes fréquents) |
| 2026-08-31 | Phase 2 (modèle) | Le modèle d'analyse (deepseek-v4-flash) ne supporte PAS l'input image : les screenshots PNG ne peuvent pas être analysés visuellement | Compenser par descriptions ASO complètes + avis (qui décrivent l'UI) + naming ; les PNG restent pour le gauntlet-loop | Toujours lire la description ASO complète + release notes ; documenter la limite dans les rapports |
| 2026-08-31 | Phase 2 (métadonnées) | Redirection PowerShell `>` écrit les JSON en UTF-16 → erreurs JSON.parse dans download-assets-ios.js | Après `node ... > fichier.json`, convertir en UTF-8 (`[IO.File]::WriteAllText` avec UTF8Encoding) avant tout script Node | Helper : `$out = node ... | Out-String; [IO.File]::WriteAllText(path, $out, UTF8)` |
